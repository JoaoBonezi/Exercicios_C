//for 
#include <stdio.h>
int main(){
    int i;

    for(i=0;i <=2; i++){ //99 pois i começa com 0. Os comandos voa ser repetidos 99 vezes.
        // i=0 é a inicialização;
        // i<=99 é a condicional;
        // i++ de quanto eu conto ( incremento );
        //break; é quando o for para de executar no momento que voce quiser;
        //continue; vai para próxima veze nao faz o processamente;
        //aqui dentro esrever códigos: scanf,printf,if(else if, else),declarar váriavel

        printf("%d\n", i);

    }
    return 0;
}